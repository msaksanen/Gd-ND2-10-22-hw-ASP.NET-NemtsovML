@using MedContactApp.Helpers;

@model IEnumerable<CustomerDto>

@{
    ViewData["Title"] = "Customers Table";
    ViewBag.pageRoute = "/index/customer";
}


<a href="~/customer/create">Add customer</a>

<Table class="delivery">   @*" table table-bordered"*@
    <caption>Customers Info Table</caption>
    <thead>
    <tr>
        <th>
           Username
        </th>
        <th>
           Email
        </th>
        <th>
           User Id
        </th>
        <th>
           Name
        </th>
        <th>
           Middle Name
        </th>
        <th>
           Surname
        </th>
        <th>
           Age
        </th>
        <th>
           Sex
        </th>
        <th>
           Address
        </th>
        <th>
           Details
        </th>
    </tr>
    <thead>
    <tbody>
    
        

@if (Model.Any())
{
    foreach (var item in Model)
    {
        await Html.RenderPartialAsync("CustomerPartialView", item);
    }
}

    </tbody>
</Table>
@Html.GeneratePagintator((int)ViewBag.pageCount, (int)ViewBag.currentPage, (string)ViewBag.pageRoute)

@section CssStyles
    {
    <style>
       caption {
          caption-side: top;
          text-align: center;
          padding: 10px 0;
          font-size: 14px;}
    </style>
}

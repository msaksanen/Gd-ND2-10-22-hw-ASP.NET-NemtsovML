@model SelectItem
@if (Model.IsSelected!=true)
{
    <option label="@Model.Name" value="@Model.IntId">@Model.Name</option>
}
else
{
    <option label="@Model.Name" selected value="@Model.IntId">@Model.Name</option>
}
